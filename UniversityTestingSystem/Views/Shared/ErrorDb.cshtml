@model System.Data.SqlClient.SqlErrorCollection

@{
    ViewBag.Title = "Ошибка БД";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Ошибка БД</h2>


@if (Model.Count != 0)
{
    <ul>
        @foreach (var error in Model)
        {
            <li>error</li>
        }
    </ul>
}
